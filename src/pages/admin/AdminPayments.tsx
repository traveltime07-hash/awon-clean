import React from "react";
export function AdminPayments() {
  // Na razie placeholder — prawdziwa wersja jest w modules/admin (poniżej)
  return (
    <div className="card">
      <h1>Płatności BLIK do weryfikacji</h1>
      <p>Lista zgłoszeń (mock). Prawdziwy komponent w <code>modules/admin/AdminPaymentsPage.tsx</code>.</p>
    </div>
  );
}
